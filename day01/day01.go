package day01

import "errors"

func partOne(data []int) (int, error) {
	var product int
	err := errors.New("no value")
	return product, err
}

func partTwo(data []int) (int, error) {
	var product int
	err := errors.New("no value")
	return product, err
}
